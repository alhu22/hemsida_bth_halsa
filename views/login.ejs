<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/login.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to bottom right, #fef3d6, #b3d4fc);
            font-family: Arial, sans-serif;
            margin: 0;
        }
        .sign-in__wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            position: relative;
        }
        .form-container {
            z-index: 2;
            width: 450px;
            background: #fff;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .footer {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            color: white;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="sign-in__wrapper">
        <!-- Login Form -->
        <div class="form-container shadow p-4 rounded">
            <!-- Header -->
            <img
                class="img-thumbnail mx-auto d-block mb-2"
                src="images/bth.png"
                style="width: 80px;"
            />
            <div class="h4 mb-3 text-center">Logga in</div>

            <form action="/home" method="POST">
                <!-- Username -->
                <div class="mb-3">
                    <label for="username" class="form-label">Användernamn</label>
                    <input
                        type="text"
                        class="form-control"
                        id="username"
                        name="username"
                        placeholder="Användernamn"
                        required
                    />
                </div>

                <!-- Password -->
                <div class="mb-3">
                    <label for="password" class="form-label">Lösningsord</label>
                    <input
                        type="password"
                        class="form-control"
                        id="password"
                        name="password"
                        placeholder="Lösningsord"
                        required
                    />
                </div>

                <!-- Remember Me -->
                <div class="mb-3 form-check">
                    <input type="checkbox" class="form-check-input" id="rememberMe" name="rememberMe">
                    <label class="form-check-label" for="rememberMe">Remember me</label>
                </div>

                <!-- Submit Button -->
                <button
                    type="submit"
                    class="btn btn-primary w-100"
                    id="loginButton"
                >
                    Logga In
                </button>

                <!-- signUpp -->
                <div class="d-grid justify-content-end mt-2">
                    <a href="/signUpp" class="text-muted text-decoration-none">Registrera</a>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
